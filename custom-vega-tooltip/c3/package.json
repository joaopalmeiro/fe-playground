{
  "name": "custom-vega-tooltip",
  "version": "0.0.1",
  "description": "Custom tooltip for Vega-Lite",
  "scripts": {
    "start": "npm run watch",
    "serve": "live-server --port=8000 --host=localhost",
    "build:sass": "sass vega-tooltip.scss:vega-tooltip.css --no-source-map",
    "watch": "npm-run-all --parallel watch:* serve",
    "watch:sass": "npm run build:sass -- --watch"
  },
  "author": "João Palmeiro",
  "license": "MIT",
  "dependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.32.8"
  }
}